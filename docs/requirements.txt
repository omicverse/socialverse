# Build dependencies for the socialverse documentation site (Sphinx + sphinx-book-theme).
# Notebooks are rendered from their committed outputs (nb_execution_mode="off"),
# so the socialverse package itself is NOT required to build the docs.
sphinx>=7.2,<8
sphinx-book-theme>=1.1.0
myst-nb>=1.1.0
sphinx-copybutton>=0.5.0
sphinx-design>=0.6.0
